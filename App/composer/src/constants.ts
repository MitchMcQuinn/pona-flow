/** Shared constants for Cypher composition. */

export const EXISTS_INSTANCE_VAR = "i";

export const GRAPH_PAYLOAD_LABELS = ["STEP", "SCHEMA"] as const;

export const DEFAULT_SCHEMA_KEY_PROPERTY_NAME = "id";
