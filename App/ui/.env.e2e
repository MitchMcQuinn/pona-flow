# Loaded by: cd App/ui && npm run dev:e2e   (vite --mode e2e)
# Keeps Clerk on localhost so Cypress can call cy.clerkSignIn() without redirecting
# to the hosted Account Portal (*.accounts.dev).
VITE_E2E_SKIP_AUTH_REDIRECT=true
