// Archived from App/authoring/src/parameterRefs.ts
            // Code-execution STEP: $param tokens in the script behave like body refs.
            addRefsFromText(String(el.node.sequencial_properties?.code ?? ""), refs);
